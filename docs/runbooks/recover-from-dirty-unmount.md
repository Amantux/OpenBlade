# Recover from dirty unmount

1. Stop new jobs.
2. Inspect drive and LTFS mount state.
3. Do not unload until the tape is unmounted cleanly.
4. If required, remount read-only to inspect contents.
5. Re-run verification before returning the tape to service.
