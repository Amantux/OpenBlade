# Simulator guide

The simulator models slots, drives, changer motion, LTFS tape contents, mount states, and injected faults. It is the default backend and is used by unit, integration, property, fault, and e2e tests.

Use `openblade mock init` to re-seed a library and `openblade mock inventory` to inspect it.
