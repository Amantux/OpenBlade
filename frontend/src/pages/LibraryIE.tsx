import StubPage from '../components/pages/StubPage';

export default function LibraryIE() {
  return (
    <StubPage
      eyebrow="Library"
      title="IE Station"
      description="Operator-focused import/export slot workflows and station controls will be expanded in the next build."
    />
  );
}
