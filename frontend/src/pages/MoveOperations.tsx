import StubPage from '../components/pages/StubPage';

export default function MoveOperations() {
  return (
    <StubPage
      eyebrow="Operations"
      title="Move Operations"
      description="Robotics move requests, destination planning, and audit-safe execution tracking are planned for the next build."
    />
  );
}
