import StubPage from '../components/pages/StubPage';

export default function InventoryScan() {
  return (
    <StubPage
      eyebrow="Operations"
      title="Inventory Scan"
      description="Partition-level scans, robotic audits, and progress telemetry will be surfaced in the next build."
    />
  );
}
