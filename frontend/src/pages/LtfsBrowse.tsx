import StubPage from '../components/pages/StubPage';

export default function LtfsBrowse() {
  return (
    <StubPage
      eyebrow="Media"
      title="LTFS Browse"
      description="Filesystem-aware LTFS browsing, mount orchestration, and content previews are planned for the next build."
    />
  );
}
