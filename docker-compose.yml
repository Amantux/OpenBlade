services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      OPENBLADE_BACKEND: mock
      OPENBLADE_DB_URL: sqlite:////data/openblade.db
      OPENBLADE_EMULATOR_URLS: ${OPENBLADE_EMULATOR_URLS:-http://host.docker.internal:8010,http://host.docker.internal:8011,http://host.docker.internal:8012}
      # AI assistant (optional): point at any OpenAI-compatible endpoint to enable
      # the in-UI helper. Left blank -> assistant stays disabled.
      OPENBLADE_ASSISTANT_BASE_URL: ${OPENBLADE_ASSISTANT_BASE_URL:-}
      OPENBLADE_ASSISTANT_MODEL: ${OPENBLADE_ASSISTANT_MODEL:-}
      OPENBLADE_ASSISTANT_API_KEY: ${OPENBLADE_ASSISTANT_API_KEY:-}
      OPENBLADE_ASSISTANT_ENABLED: ${OPENBLADE_ASSISTANT_ENABLED:-}
    volumes:
      - openblade-data:/data
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 10s
      timeout: 5s
      retries: 5

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    depends_on:
      api:
        condition: service_healthy

volumes:
  openblade-data:
